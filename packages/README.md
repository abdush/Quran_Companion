# packages/

Shared TypeScript packages (handbook §21). pnpm workspace members once each gains a `package.json`.

| Package | Owner |
|---|---|
| `quran-core/` | Renderer Agent (`agents/renderer.md`) |
| `mushaf-renderer/` | Renderer Agent (`agents/renderer.md`) |
| `fsrs-engine/` | Backend + Testing Agents (task 1.5; final ownership via Architecture Agent) |
| `tajweed-rules/` | Unassigned — Architecture Agent to arbitrate |
| `sync-client/` | Sync Agent (`agents/sync.md`) |
| `api-client/` | Generated from `schemas/openapi` — regenerated by codegen, never hand-edited |
| `ai-core/` | AI Agent (`agents/ai.md`) |
| `auth-core/` | Auth/Security Agent (`agents/security.md`) |
| `ui/` | Frontend Agent (`agents/frontend.md`) — RTL-first design system |
| `plugin-sdk/` | Unassigned — Architecture Agent to arbitrate |

> Placeholder created by Task 0.1 (DevOps). Owning agents scaffold their own packages.

/**
 * @qc/api-client — stub (Task 0.2).
 *
 * Only generated types are exported for now; the runtime client (fetch layer,
 * ETag revalidation, problem+json handling) lands with task 1.2. Feature code
 * must consume this package rather than hand-writing fetch calls (§5.3).
 */
export type * as qds from './generated/qds.js';
export type * from './generated/events.js';
export type * from './generated/packs.js';
export type * from './generated/licenses.js';

# docs/

Owner: **Documentation Agent** (`agents/documentation.md`), except `adr/` which is owned by the Architecture Agent.

The full documentation set is specified in handbook §22. Docs are updated in the same PR as the change they describe (Rule R5); CI enforces doc-touch heuristics.

> Placeholder created by Task 0.1 (DevOps). `SELF_HOSTING.md` was seeded by DevOps because task 0.1 introduced the compose stack (R5); Documentation Agent owns it going forward.
